<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.telas_background">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home"
                android:screenOrientation="nosensor"></activity>
        <activity android:name=".Comentarios_Posts" />
        <activity android:name=".Perfil" />
        <activity android:name=".Editar_perfil" />
        <activity android:name=".Encontro" />
        <activity android:name=".Friend_Request" />
        <activity android:name=".Chat" />
        <activity android:name=".Conversas" />
        <activity android:name=".Registrar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>